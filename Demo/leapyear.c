#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{

    int n,a;
    scanf("%d", &n);
  	a= (n*2)-1;
     
      for (int i=1;i<=a;i++){
          printf("%d",n);
      }
    return 0;
}